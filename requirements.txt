# Intentionally empty. The LCT battle-report tooling is standard-library only
# (Python 3.13). This file exists so Render's Python runtime detects the
# service; `pip install -r` on a comment-only file is a successful no-op.
